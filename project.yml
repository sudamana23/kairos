name: Kairos
options:
  bundleIdPrefix: com.damianspendel
  createIntermediateGroups: true

schemes:
  Tenets:
    build:
      targets:
        Kairos: all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

targets:
  Kairos:
    type: application
    platform: macOS
    deploymentTargets:
      macOS: "15.1"
    sources:
      - path: Kairos
    dependencies:
      - sdk: Security.framework
      - sdk: Speech.framework
      - sdk: UserNotifications.framework
    settings:
      base:
        PRODUCT_NAME: Tenets
        PRODUCT_BUNDLE_IDENTIFIER: com.damianspendel.kairos
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "15.1"
        ENABLE_HARDENED_RUNTIME: YES
        "ENABLE_APP_SANDBOX[sdk=macosx*]": YES
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_IDENTITY: "Apple Development"
        DEVELOPMENT_TEAM: 5Y9SADJQM9
        CODE_SIGN_ENTITLEMENTS: Kairos/Resources/Kairos.entitlements
        CODE_SIGN_ALLOW_ENTITLEMENTS_MODIFICATION: YES
        ARCHS: "$(ARCHS_STANDARD)"
        ONLY_ACTIVE_ARCH: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        SWIFT_STRICT_CONCURRENCY: targeted
    info:
      path: Kairos/Resources/Info.plist
      properties:
        CFBundleName: Tenets
        CFBundleDisplayName: Tenets
        LSApplicationCategoryType: public.app-category.productivity
        CFBundleShortVersionString: "1.0.0"
        CFBundleVersion: "25"
        NSHighResolutionCapable: true
        ITSAppUsesNonExemptEncryption: false
        LSMinimumSystemVersion: "$(MACOSX_DEPLOYMENT_TARGET)"
        NSMicrophoneUsageDescription: "Tenets uses the microphone for voice pulse check-ins and monthly review recordings."
        NSSpeechRecognitionUsageDescription: "Tenets transcribes your voice notes on-device for weekly pulse check-ins."
        CFBundleURLTypes:
          - CFBundleURLSchemes:
              - kairos
            CFBundleURLName: com.damianspendel.kairos
            CFBundleTypeRole: Viewer
        UTExportedTypeDeclarations:
          - UTTypeIdentifier: com.damianspendel.kairos.domain-drop
            UTTypeDescription: Kairos Domain Card
            UTTypeConformsTo:
              - public.data
          - UTTypeIdentifier: com.damianspendel.kairos.kr-drop
            UTTypeDescription: Kairos Key Result
            UTTypeConformsTo:
              - public.data
